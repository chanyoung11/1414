# 콘티 — 개발자 인수인계

작성 2026-09-03 · 보내는 사람: 하은(PM) · 받는 사람: 개발 담당
번들 구성: 이 문서 · `app/` (지금 돌아가는 프로토타입, 배포 가능 상태) · `docs/` (기능명세서·화면 시각화) · `tests/` (자동 테스트)

## 0. 한 줄 요약

찬양팀 인계 도구 프로토타입이 **기기 안에서 완전히 동작하는 상태**까지 와 있습니다. 파일 하나(`index.html`, 938줄, 프레임워크·빌드 없음)이고, 인도자가 콘티를 만들고 → 발행하고 → 멤버가 보고 → 연습·무대에서 쓰는 루프가 전부 됩니다. 지금 없는 건 딱 하나, **사람들 사이를 잇는 서버**입니다. 부탁하는 일은 ① 계정·팀 가입 ② 발행본과 메모의 동기화 ③ 배포입니다. UI와 동작은 PM과 여러 번 돌려 확정한 것이라 **다시 만들지 말고 그 위에 붙여 주세요.**

## 1. 지금 되는 것

- 설정: 이름·세션·역할(인도자/멤버), 팀 세션 목록, 상용구
- 에디터(인도자): 예배 이름·날짜·전체 공지, 곡 순서(끌어서 정렬), 라이브러리 재사용, 곡 제목, 키·전조, 송폼 한 줄 입력 → 파서가 칩으로 표시, 유튜브 링크(시작·끝 구간 지정), 녹음 파일, 타임라인 메모(시각·세션·텍스트), 곡 메모, 섹션 메모
- 악보: 이미지 업로드(카메라·스크린샷·스캔) → 자동 정리(기울기 보정, 명암 평탄화, 여백 트림, 줄 사이 여백 인식) → 탭으로 마커, 마커 위 여백에 절단선 자동 스냅(끌어서 조정), 하이라이트 사각형
- 메모 시스템: 인도자/세션/개인 3층, 마커에 붙는 섹션 메모는 악보 줄 사이 "띠"로 표시, 2개+N, 여백이 없는 마커는 배지 모드
- 발행: 버전 +1, 스냅샷, 카톡용 텍스트 자동 생성(변경 요약 포함), 파일 내보내기
- 콘티 보기(멤버): 세션 필터, 곡 카드, 유튜브 시각 딥링크
- 연습·무대 화면: 좌우 분할(폭·높이 드래그 조절, 기기에 기억), 미디어(유튜브 임베드 postMessage 제어 / 녹음 재생), 큰 컨트롤, 진행바에 메모 눈금, "여기에 메모", 곡 이동 캐러셀, 층별 메모 필터, 무대 모드(전체화면, 상단은 노란 송폼 바만)
- 데이터: IndexedDB(이미지·녹음은 Blob, 나머지는 JSON), 파일 내보내기/가져오기(멤버 개인 메모는 새 버전 가져와도 유지)
- PWA: `sw.js`로 오프라인(한 번 열면 네트워크 없이 열림), 홈 화면 추가용 manifest·아이콘

자동 테스트(`tests/`)로 위 루프 전체가 헤드리스 크로미움에서 통과합니다. 실제 악보 이미지로 마커 → 절단선 → 띠 → 발행 → 내보내기 → 다른 컨텍스트에서 가져오기까지.

## 2. 부탁하는 것 (우선순위 순)

### A. 계정·팀
- 로그인: **카카오 로그인 권장** (팀 전원이 카톡 사용). 이메일 매직링크도 무방.
- 팀: 인도자가 팀을 만들면 초대 링크(토큰)가 생기고, 멤버는 링크로 들어와 이름·세션만 고르면 가입. 지금 앱의 "설정" 화면이 그 역할이라 UI는 거의 그대로.
- 역할: 인도자(팀 관리·콘티 제작·발행), 세션리더(자기 세션에 메모 공유), 멤버.

### B. 동기화
- 인도자가 **발행**하면 그 예배의 스냅샷(JSON)과 첨부(이미지·녹음)가 서버에 올라가고, 멤버는 앱을 열면 최신 발행본을 받습니다. 지금은 이 자리를 파일 내보내기/가져오기가 대신하고 있습니다. `exportJSON()` / `importJSON()`이 그대로 업로드/다운로드 함수의 원형입니다.
- 멤버 메모(섹션 메모·타임라인 메모)는 행 단위로 서버에 쓰고, 다른 멤버의 "세션 공유" 메모는 같은 세션에게만 내려옵니다.
- 오프라인은 유지: 지금처럼 IndexedDB를 캐시로 쓰고, 오프라인에서 쓴 메모는 큐에 두었다가 올립니다. 무대에서 네트워크가 끊겨도 악보가 떠야 합니다.
- 카톡엔 링크 하나: 발행 시 만들어지는 카톡 텍스트의 `👉` 자리에 예배 링크(`https://…/#/view/{id}`)가 들어가면 됩니다. `kakaoText()` 참조.

### C. 배포
- 정적 호스팅(Netlify/Cloudflare Pages/Vercel 아무거나). `app/` 폴더를 그대로 올리면 됩니다. https 필수(카메라·마이크·전체화면·서비스워커 조건).
- **유튜브 임베드는 진짜 도메인에서만 됩니다.** claude.ai 링크나 file://에서는 리퍼러가 없어 오류 153. iframe에 `referrerpolicy="strict-origin-when-cross-origin"`은 이미 넣어뒀습니다. 서버 응답 헤더에 `Referrer-Policy: no-referrer`만 붙이지 마세요.
- iOS는 첫 재생을 영상 자체에서 탭해야 할 수 있습니다(자동재생 정책). 이후엔 앱 버튼으로 제어됩니다.

## 3. 코드 지도 (`app/index.html`)

프레임워크 없음. 전역 상태 `S` 하나를 JSON으로 IndexedDB에 저장하고, 해시 라우팅으로 화면을 통째로 다시 그립니다. 함수 67개, `data-act` 액션 36개. 위에서 아래로:

| 구역 | 주요 함수 | 역할 |
|---|---|---|
| utils | `uid esc clone fmtT parseT toast` | 공용 |
| IDB | `IDB.open/get/put/del`, `blobURL()` | 저장소. `kv`(state JSON), `blobs`(이미지·녹음) |
| state | `S`, `save()`, `newService() newItem()`, `keyStr()` | 데이터 모델은 §4 |
| icons | `ICONS`, `ic(name)` | 인라인 SVG. 외부 의존 없음 |
| 송폼 파서 | `parseForm(str)`, `chipsHTML()`, `modsFor()` | `" – "`로 세그먼트, 섹션/파트/흐름/수식어 토큰. 실패해도 원문은 그대로 보관 |
| 악보 처리 | `importImage()`, `analyze()`, `flatten()`, `rotateCanvas()`, `inkBox()`, `cropCanvas()`, `detectGaps()`, `autoCut()` | 전부 클라이언트 canvas. 여백 감지 = 행별 잉크 비율. 음표 인식 없음 |
| 메모 | `notesOfMarker() noteVisible() noteChip()` | 3층 × 필터 규칙은 `noteVisible()` 한 곳 |
| 악보 렌더 | `sheetHTML(item,W,ctx)`, `stripHTML()` | 조각을 절단선에서 잘라 슬라이스로 그리고 사이에 띠 삽입. 마커·하이라이트는 조각 좌표 → 표시 좌표 변환 |
| 라우팅 | `route() go() render()` | `#/home` `#/edit/{sid}` `#/view/{sid}` `#/play/{sid}/{idx}` |
| 홈 | `homeHTML()` | |
| 에디터 | `editorHTML() itemEditorHTML() afterEditor() drawEditorSheet() initSongDrag() labelPicker() markerMenu()` | 입력 바인딩은 `data-f`, 절단선 드래그는 `afterEditor` 안의 캡처 리스너 |
| 작성창 | `openComposer(svc,item,{marker,leader,t,mediaId})` | 섹션 메모·타임라인 메모 공용 |
| 발행 | `publishModal() kakaoText() diffSvc()` | 스냅샷 = `clone(items)`에서 notes 제거 |
| 뷰어 | `viewerHTML()` | 발행본이 있으면 발행본, 없으면 초안(표시) |
| 연습·무대 | `playHTML() afterPlay() redrawPlay() selectMedia() updateTL()`, 플레이어 객체 `P`, 상태 `PL`, `setStage()` | 유튜브는 IFrame API 스크립트 없이 postMessage(`listening` / `command`)로 제어 |
| 설정·라이브러리·파일 | `settingsModal() libraryPicker() exportJSON() importJSON()` | 가져오기 병합 규칙: 같은 예배면 내 메모(layer≠leader, author=나)는 마커 id 기준으로 유지 |
| 워크스페이스 | `ptab() initGutter() initVGutter()` | 프리미어식 패널, 좌우·상하 드래그, localStorage에 기억 |
| 부트 | 맨 아래 `(async function boot(){…})()` | 마이그레이션(`keyNow`→`mod`)도 여기 |

CSS는 `<style>` 한 블록. `:root` 토큰(다크 팔레트, 노란 `--amber`, 청록 `--teal`)과 `.sheet` 안에서만 라이트 팔레트로 재정의(악보는 항상 흰 종이).

## 4. 데이터 모델 (그대로 테이블로 옮기면 됩니다)

```
Team     { sessions[], phrases[], me{name, session, role} }     ← me는 로그인으로 대체
Service  { id, name, date, notice, version, items[], published{version, at, name, date, notice, items[]} }
Item     { id, title, key, mod, form, songNote, libId, pieces[], media[], notes[] }
Piece    { id, blob, w, h, gaps[{s,e}], markers[{id,label,x,y,cut}], hls[{id,x,y,w,h}] }
Media    { id, type:'youtube'|'audio', url|blob, name, start, end, notes[] }
Note     { id, marker, layer:'leader'|'session'|'mine', session|null, text, author, at }          (섹션 메모, Item.notes)
TLNote   { id, t, layer, session|null, text, author, marker? }                                      (타임라인 메모, Media.notes)
Library  Song = Item과 같은 모양 (notes 없음)
Blob     id → 이미지(JPEG, 최대 폭 1400) 또는 녹음 파일
```

서버 설계 제안(2인 팀 기준으로 가장 빠른 길 — Supabase: Auth + Postgres + Storage + Realtime):

- `teams(id, name, sessions jsonb, phrases jsonb, invite_token)`
- `members(user_id, team_id, name, sessions text[], role)`
- `services(id, team_id, doc jsonb, version, published_at)` — **인도자 소유 JSON을 통째로.** 클라이언트가 이미 이 모양으로 일하므로 정규화는 나중에.
- `notes(id, team_id, service_id, item_id, marker_id|null, media_id|null, t|null, layer, session, text, author_id, created_at)` — 멤버가 쓰는 유일한 테이블. RLS: 같은 팀만 읽기, 본인 것만 쓰기·삭제, 인도자는 전부 삭제 가능.
- `library(id, team_id, doc jsonb)`
- Storage 버킷 `team-{id}` 비공개, 서명 URL. 이미지는 클라이언트가 이미 1400px JPEG로 줄여서 올립니다. 녹음은 용량 상한(예: 60MB)만.
- 동기화 흐름: 발행 = `services.doc` upsert + 새 blob 업로드. 멤버 앱 열기 = `services` 최신본 fetch + 내 팀 `notes` fetch → 로컬 IndexedDB에 캐시 → 지금의 렌더 코드 그대로. Realtime은 있으면 좋고 없어도 열 때 갱신으로 충분.
- 발행본 링크: `#/view/{service_id}`.

## 5. 손대지 말아야 할 것 / 알아둘 것

- 송폼 표기와 마커 라벨은 팀이 실제 쓰는 표기입니다. 파서를 "정리"하지 마세요. 인식 못 한 조각은 원문 칩으로 남기는 게 의도입니다.
- 악보는 절대 좌표가 아니라 **마커**에 메모가 붙습니다. 그래서 새 버전이 발행돼도 개인 메모가 살아남습니다. 마커 id는 발행 간에 유지되어야 합니다(지금 코드가 그렇게 합니다).
- 악보 이미지는 팀 내부 비공개 자료로만 다룹니다. 검색이나 공유 라이브러리를 만들지 마세요(저작권).
- 유튜브 IFrame API 스크립트를 로드하지 않고 postMessage로 제어하는 건 의도입니다(외부 스크립트 의존 없이 오프라인 셸 유지).
- 폰트·아이콘 전부 인라인. CDN 넣지 않는 게 원칙.

## 6. 남아 있는 TODO (서버 뒤에)

1. PDF 가져오기(pdf.js로 페이지 → 이미지 → 같은 파이프라인)
2. 수동 크롭(자동 트림이 못 잡는 경우), 절단선 없는 마커의 UX 다듬기
3. 내보내기 파일에서 녹음 제외 옵션(서버가 붙으면 필요 없어짐)
4. 라이브러리 편집·삭제
5. 같은 라벨 마커 중복 경고
6. 읽음 확인, PDF 내보내기(인쇄용, 한 장 2곡)
7. 채보(OMR)는 **하지 않음.** 검토했고 이 단계 범위 밖. 필요해지면 서버에 Audiveris → MusicXML → OSMD 렌더 순.

## 7. 인수 기준 (이게 되면 끝)

1. 인도자가 카카오로 로그인해 팀을 만들고 초대 링크를 카톡에 붙인다.
2. 멤버 7명이 링크로 들어와 이름·세션을 고른다.
3. 인도자가 곡 4개짜리 콘티를 만들어 발행한다. 카톡 텍스트에 링크가 들어 있다.
4. 멤버가 링크를 열면 콘티 보기가 뜨고, 자기 세션 메모만 보이고, 유튜브가 앱 안에서 재생된다.
5. 멤버가 연습 화면에서 메모를 쓰면 같은 세션 사람에게 보인다. 인도자가 v2를 발행해도 그 메모가 남아 있다.
6. 기내 모드로 무대 모드를 열어 악보를 끝까지 스크롤한다.
7. `tests/test_app.py`가 통과한다(서버 붙이면 로그인 단계만 추가).

## 8. 테스트 돌리기

```
pip install playwright && playwright install chromium
python tests/test_app.py     # 전체 루프 (에디터 → 발행 → 뷰어 → 연습 → 멤버 가져오기)
python tests/test_tl.py      # 유튜브 타임라인 (임베드 없는 환경 포함)
```
파일 경로 상수(`URL`, 악보 이미지)만 자기 환경에 맞게 바꾸면 됩니다. 로컬 확인은 `cd app && python -m http.server 8765` 후 `http://localhost:8765/`.

## 9. 참고 문서

- `docs/기능명세서_v1.md` — 화면·기능 정의(줄글). 이 문서와 어긋나면 실제 앱이 우선(명세 이후 UI가 다크·패널 구조로 바뀜).
- `docs/세부시각화_v1.html` — 초기 와이어프레임. 구조 이해용.
- `docs/여백감지_테스트.jpg` — 줄 사이 여백 감지 결과 샘플.
