# 구글·애플 로그인 (2026-09-20)

## 서버
- `identities(provider, subject, user_id, email)` — 사람을 찾는 열쇠는 (provider, subject)
- `POST /auth/social {provider, idToken, name?, agreedAt?}`
  - 로그인 상태로 부르면 **계정 연결**, 아니면 찾거나 새로 만든다
  - 소셜로만 가입하면 `password_hash=''` → 비밀번호로는 못 들어온다
- `GET /auth/social` 붙은 목록 · `DELETE /auth/social/:provider` 떼기
  (마지막 로그인 수단은 못 뗀다)
- `GET /auth/providers` (공개) — 화면이 어떤 버튼을 띄울지 알려 준다
- `POST /auth/apple/callback` — 애플 웹 팝업이 돌아오는 자리
- 검증은 `lib/social.js`: 제공자 JWKS 로 서명·iss·aud·exp. Web Crypto 만 써서
  Workers·Node 양쪽에서 돈다

## 구글 (Google Cloud 프로젝트 1414)
OAuth 동의 화면: 외부, 지원 이메일 chanyoung07119@gmail.com

| 용도 | SHA-1 / 정보 | 클라이언트 ID 끝부분 |
|---|---|---|
| 웹 | lets1414.com, www, localhost:8766 | …v8tr08d5… |
| iOS | 번들 com.lets1414.app · 팀 S26G86V3G7 | …t9mgtulg… |
| 안드로이드 디버그 | A8:51:EE:F2:… | …bic2ppjl… |
| 안드로이드 업로드 키 | A1:F5:0E:04:… | …hdk9hj6n… |
| 안드로이드 플레이 서명 | AA:8C:09:37:… | …39veq7vf… |

안드로이드는 **패키지명+SHA-1** 로 앱을 가리므로 클라이언트 ID 를 코드에 넣지 않는다.
앱이 실제로 쓰는 값은 `webClientId`(웹 것)이고, 그래서 안드로이드에서 받은 토큰의
aud 는 웹 클라이언트 ID 다. 서버가 세 개를 모두 허용한다.

플레이 앱 서명 키 SHA-1 을 구하는 법: 플레이 콘솔 → Google Play로 보호됨 → 앱 서명 →
인증서 다운로드 → `deployment_cert.der` → `keytool -printcert -file`.
(zip 안에 hybrid_* 도 들어 있는데 그건 양자 내성 키라 로그인과 무관하다.
SHA-256 이 assetlinks.json 의 값과 같은 것이 진짜 배포 키다)

## 애플
- App ID `com.lets1414.app` 에 Sign in with Apple 켬
- Services ID `com.lets1414.web` — 도메인 `lets1414.com,www.lets1414.com`(쉼표로!),
  Return URL `https://lets1414.com/api/auth/apple/callback`
- 키 `4FSZRYFC53` (.p8) — Cloudflare secret 에만 있다
- iOS 앱: entitlements `com.apple.developer.applesignin`,
  Info.plist 에 구글 URL 스킴(`com.googleusercontent.apps.…`)

## 앱 (@capgo/capacitor-social-login 8.5.10)
- 안드로이드에 `apple` 설정을 넘기면 `redirectUrl` 을 요구하니 쓰는 쪽만 넘긴다
- `scopes` 를 주면 MainActivity 를 고치라고 막는다 → 기본값으로 충분해서 안 준다
- 안드로이드에는 애플 버튼을 띄우지 않는다
